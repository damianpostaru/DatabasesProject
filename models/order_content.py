# from modules.mysql_model import db
#

#
#
# class OrderContent(db.Model):
#     id = db.Column(db.Integer, primary_key=True, autoincrement=True)
#     pizzas = db.relationship('Pizza', secondary=pizzas, backref=db.backref('pizzas', lazy=True))
#
#
# db.create_all()
